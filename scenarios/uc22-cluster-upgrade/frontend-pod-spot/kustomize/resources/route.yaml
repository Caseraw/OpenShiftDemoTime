---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend-pod-spot
spec:
  to:
    kind: Service
    name: frontend-pod-spot
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
