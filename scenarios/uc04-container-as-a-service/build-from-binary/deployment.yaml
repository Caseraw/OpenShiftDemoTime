kind: Deployment
apiVersion: apps/v1
metadata:
  name: demo-binary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-binary
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: demo-binary
        deployment: demo-binary
    spec:
      containers:
        - name: demo-binary
          image: 'image-registry.openshift-image-registry.svc:5000/demo/demo-binary'
          ports:
            - containerPort: 8080
              protocol: TCP
            - containerPort: 8443
              protocol: TCP
          env:
            - name: BUILD
              value: Binary
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600