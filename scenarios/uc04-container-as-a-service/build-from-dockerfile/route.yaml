kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: demo-dockerfile
spec:
  to:
    kind: Service
    name: demo-dockerfile
    weight: 100
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None