kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: demo-source
spec:
  to:
    kind: Service
    name: demo-source
    weight: 100
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None