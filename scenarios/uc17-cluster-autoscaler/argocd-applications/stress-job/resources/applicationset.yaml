---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: stress-job
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: stress-job-placement
        requeueAfterSeconds: 10
  template:
    metadata:
      name: stress-job-{{name}}
      annotations:
        apps.open-cluster-management.io/ocm-managed-cluster: "{{name}}"
        apps.open-cluster-management.io/ocm-managed-cluster-app-namespace: openshift-gitops
        argocd.argoproj.io/skip-reconcile: "true"
      labels:
        apps.open-cluster-management.io/pull-to-ocm-managed-cluster: "true"
        velero.io/exclude-from-backup: "true"
    spec:
      destination:
        namespace: stress-job
        server: "{{server}}"
      project: default
      sources:
        - path: scenarios/uc17-cluster-autoscaler/stress-job/kustomize
          repoURL: https://github.com/Caseraw/OpenShiftDemoTime.git
          targetRevision: HEAD
          repositoryType: git
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
